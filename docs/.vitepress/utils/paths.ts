import { resolve } from 'path'

export const proJRoot = resolve(__dirname, '../../../')

export const docRoot = resolve(proJRoot, 'docs')

export const utilRoot = resolve(docRoot, '.vitepress', 'utils')

